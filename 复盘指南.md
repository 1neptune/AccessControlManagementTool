# 访问控制配置工具 - 复盘指南

## 项目简介

访问控制配置工具是一款基于 Go 语言开发的桌面应用程序，用于管理服务器的访问控制策略，支持 Linux 和 Windows 系统的防火墙配置。

## 目录结构

```
访问控制配置工具/
├── cmd/                              # 编译入口目录
│   ├── icons/                        # 应用图标资源
│   │   ├── logo_16.png
│   │   ├── logo_32.png
│   │   ├── logo_48.png
│   │   ├── logo_64.png
│   │   ├── logo_128.png
│   │   └── logo_256.png
│   ├── winres/                       # Windows 资源配置
│   │   ├── icon.png
│   │   ├── icon16.png
│   │   └── winres.json
│   ├── logo.ico                      # 应用图标
│   ├── logo.png                      # Logo 图片
│   ├── main.go                       # 程序入口函数
│   ├── rsrc_windows_386.syso         # 32位 Windows 资源文件
│   └── rsrc_windows_amd64.syso       # 64位 Windows 资源文件
│
├── internal/                         # 内部模块目录
│   ├── db/                           # 数据库访问层
│   │   └── db.go                     # SQLite 数据库操作
│   │
│   ├── detector/                     # 系统检测模块
│   │   ├── detector.go               # 系统检测接口
│   │   ├── detector_linux_impl.go    # Linux 系统检测实现
│   │   └── detector_windows.go       # Windows 系统检测实现
│   │
│   ├── firewall/                     # 防火墙配置模块
│   │   ├── firewall.go               # 防火墙配置接口
│   │   ├── firewall_linux_impl.go    # Linux iptables/ipset 配置实现
│   │   └── firewall_windows.go       # Windows IPsec 策略配置实现
│   │
│   ├── models/                       # 数据模型
│   │   └── models.go                 # Server、PortInfo 等结构体定义
│   │
│   ├── portscanner/                  # 端口扫描模块
│   │   ├── portscanner.go            # 端口扫描接口
│   │   ├── portscanner_linux_impl.go # Linux 端口扫描实现
│   │   └── portscanner_windows.go    # Windows 端口扫描实现
│   │
│   ├── service/                      # 业务逻辑层
│   │   └── service.go                # 服务器管理服务接口及实现
│   │
│   ├── smb/                          # SMB 客户端模块
│   │   └── smb.go                    # Windows 远程管理（SMB/WMI）
│   │
│   ├── ssh/                          # SSH 客户端模块
│   │   └── ssh.go                    # Linux 远程命令执行（SSH）
│   │
│   ├── ui/                           # 用户界面模块
│   │   ├── components.go             # 通用 UI 组件
│   │   ├── config_window.go          # 配置窗口
│   │   ├── logo.png                  # UI Logo
│   │   ├── mainwindow.go             # 主窗口
│   │   ├── pagination.go             # 分页组件
│   │   ├── search_panel.go           # 搜索面板
│   │   ├── server_dialog.go          # 服务器添加/编辑对话框
│   │   ├── server_table.go           # 服务器列表表格
│   │   ├── theme.go                  # 主题配置
│   │   └── window_pos.go             # 窗口位置记录
│   │
│   └── utils/                        # 工具函数
│       ├── conn.go                   # 连接工具
│       └── crypto.go                 # 加密工具（密码加密解密）
│
├── Linux访问控制流程图.png           # Linux 配置流程图
├── Linux配置访问控制策略指南.md       # Linux 配置指南
├── Windows配置访问控制流程图.md       # Windows 配置流程图
├── Windows配置访问控制策略指南.md     # Windows 配置指南
├── data.db                           # SQLite 数据库文件
├── debug.log                         # 调试日志文件
├── go.mod                            # Go 模块依赖
├── go.sum                            # 依赖校验和
├── logo.ico                          # 应用图标
├── logo.png                          # Logo 图片
└── 访问控制配置工具.exe               # 编译输出的可执行文件
```

## 编译命令

### 标准编译（压缩体积）

```powershell
$env:GOTMPDIR="E:\go_temp"; go build -ldflags "-linkmode internal -s -w -H windowsgui" -o "访问控制配置工具.exe" ./cmd
```

### 参数说明

| 参数 | 说明 |
|------|------|
| `$env:GOTMPDIR="E:\go_temp"` | 设置 Go 临时目录，避免磁盘空间不足问题 |
| `-linkmode internal` | 使用内部链接模式，减少外部依赖 |
| `-s` | 移除符号表，减小可执行文件体积 |
| `-w` | 移除 DWARF 调试信息，减小可执行文件体积 |
| `-H windowsgui` | 指定 Windows GUI 子系统，运行时不显示 CMD 窗口 |
| `-o "访问控制配置工具.exe"` | 指定输出文件名为中文 |
| `./cmd` | 指定编译入口目录 |

### 编译注意事项

1. 确保 Go 环境已正确配置（建议 Go 1.20+）
2. Windows 下使用 PowerShell 执行命令
3. 编译输出文件位于项目根目录
4. `-s -w` 参数会显著减小程序体积，但会丢失调试信息
